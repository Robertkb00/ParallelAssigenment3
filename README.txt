TO RUN:
Requires Visual Studio Code. Copy repository into Visual Studio Code, and ensure you have enabled the C++ extnesion. Then, open the Developer Command Prompt for VS. Navigate to where you have set the folder for the repository, and input 'cl /EHsc main.cpp'. Both the 'presents' and 'temperature' functions should then run.
